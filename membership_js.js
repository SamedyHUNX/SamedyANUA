// const carouselSlides = document.querySelectorAll('.carousel-slide');
// const navigationDots = document.querySelectorAll('.dot');

// let currentSlide = 0; // Keeps track of the current slide

// function updateCarousel() {
// carouselSlides.forEach((slide) => slide.classList.remove('active')); // Remove active class from all slides
// navigationDots.forEach((dot) => dot.classList.remove('active')); // Remove active class from all dots

// carouselSlides[currentSlide].classList.add('active'); // Add active class to current slide
// navigationDots[currentSlide].classList.add('active'); // Add active class to current dot
// }

// function changeSlide(slideIndex) {
// currentSlide = slideIndex - 1; // Adjust for zero-based indexing
// updateCarousel();
// }

// navigationDots.forEach((dot, index) => {
// dot.addEventListener('click', () => {
//     changeSlide(index + 1); // Adjust for zero-based indexing
// });
// });
